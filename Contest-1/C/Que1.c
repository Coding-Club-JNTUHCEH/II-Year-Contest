// Approach -> We need to print the total number of chocolates present i.e the SUM of n and m

#include <stdio.h>

int main() {
  int n, m;
  scanf("%d %d", &n, &m);
  printf("%d\n", n + m);
  return 0;
}
